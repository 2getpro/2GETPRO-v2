# 📘 Руководство по установке 2GETPRO v2 на Ubuntu 24.04.03 LTS

Это подробное пошаговое руководство по установке Telegram-бота 2GETPRO v2 на сервер Ubuntu 24.04.03 LTS. Инструкция написана простым языком и подходит даже для людей без технического опыта.

---

## 📋 Содержание

1. [Предварительные требования](#1-предварительные-требования)
2. [Подготовка сервера](#2-подготовка-сервера)
3. [Установка бота](#3-установка-бота)
4. [Настройка базы данных](#4-настройка-базы-данных)
5. [Конфигурация бота](#5-конфигурация-бота)
6. [Настройка systemd сервиса](#6-настройка-systemd-сервиса)
7. [Первый запуск](#7-первый-запуск)
8. [Настройка веб-хуков](#8-настройка-веб-хуков-опционально)
9. [Мониторинг и обслуживание](#9-мониторинг-и-обслуживание)
10. [Решение типичных проблем](#10-решение-типичных-проблем)

---

## 1. Предварительные требования

### Что вам понадобится:

✅ **Сервер с Ubuntu 24.04.03 LTS**
- Минимум 2 ГБ RAM (рекомендуется 4 ГБ)
- Минимум 20 ГБ свободного места на диске
- Доступ к интернету

✅ **Доступ к серверу**
- SSH-доступ с правами root или sudo
- IP-адрес сервера

✅ **Данные для настройки**
- Токен Telegram-бота (получить у [@BotFather](https://t.me/BotFather))
- Ваш Telegram User ID (узнать у [@userinfobot](https://t.me/userinfobot))
- API ключи платежных систем (YooKassa, CryptoPay и т.д.)
- URL и ключ API вашей панели управления VPN

💡 **Совет**: Запишите все эти данные в текстовый файл - они понадобятся при настройке.

---

## 2. Подготовка сервера

### 2.1. Подключение к серверу

Подключитесь к серверу через SSH:

```bash
ssh root@ваш_ip_адрес
```

**Что делает команда**: Устанавливает защищенное соединение с вашим сервером.

**Ожидаемый результат**: Вы увидите приглашение командной строки сервера (например, `root@server:~#`).

⚠️ **Если не получается подключиться**: Проверьте правильность IP-адреса и убедитесь, что у вас есть SSH-ключ или пароль.

---

### 2.2. Обновление системы

Обновите список пакетов и установленное ПО:

```bash
apt update && apt upgrade -y
```

**Что делает команда**: 
- `apt update` - обновляет список доступных пакетов
- `apt upgrade -y` - устанавливает все обновления (флаг `-y` автоматически подтверждает установку)

**Ожидаемый результат**: Система загрузит и установит обновления. Это может занять 5-10 минут.

💡 **Совет**: Если система попросит перезагрузку, выполните `reboot` и подключитесь заново.

---

### 2.3. Установка Python 3.11+

Ubuntu 24.04 уже включает Python 3.12, но давайте убедимся и установим необходимые компоненты:

```bash
apt install -y python3 python3-pip python3-venv python3-dev
```

**Что делает команда**: Устанавливает Python 3, менеджер пакетов pip, инструмент для создания виртуальных окружений и файлы разработки.

Проверьте версию Python:

```bash
python3 --version
```

**Ожидаемый результат**: Должна отобразиться версия Python 3.11 или выше (например, `Python 3.12.3`).

---

### 2.4. Установка PostgreSQL

PostgreSQL - это база данных для хранения информации о пользователях и транзакциях.

```bash
apt install -y postgresql postgresql-contrib
```

**Что делает команда**: Устанавливает PostgreSQL и дополнительные модули.

Запустите и включите автозапуск PostgreSQL:

```bash
systemctl start postgresql
systemctl enable postgresql
```

**Что делает команда**: 
- `start` - запускает службу PostgreSQL
- `enable` - настраивает автоматический запуск при загрузке системы

Проверьте статус:

```bash
systemctl status postgresql
```

**Ожидаемый результат**: Вы должны увидеть `active (running)` зеленым цветом.

💡 **Совет**: Нажмите `q` для выхода из просмотра статуса.

---

### 2.5. Установка Redis

Redis используется для кэширования и повышения производительности бота.

```bash
apt install -y redis-server
```

**Что делает команда**: Устанавливает Redis - систему кэширования в памяти.

Запустите и включите автозапуск Redis:

```bash
systemctl start redis-server
systemctl enable redis-server
```

Проверьте работу Redis:

```bash
redis-cli ping
```

**Ожидаемый результат**: Должно вывести `PONG`.

⚠️ **Если Redis не отвечает**: Проверьте статус службы командой `systemctl status redis-server`.

---

### 2.6. Установка Git

Git нужен для загрузки кода бота с GitHub.

```bash
apt install -y git
```

**Что делает команда**: Устанавливает систему контроля версий Git.

Проверьте установку:

```bash
git --version
```

**Ожидаемый результат**: Отобразится версия Git (например, `git version 2.43.0`).

---

### 2.7. Установка дополнительных системных пакетов

Установите необходимые библиотеки для работы бота:

```bash
apt install -y build-essential libssl-dev libffi-dev libpq-dev curl wget
```

**Что делает команда**: Устанавливает:
- `build-essential` - инструменты для компиляции
- `libssl-dev` - библиотеки для шифрования
- `libffi-dev` - библиотеки для работы с внешними функциями
- `libpq-dev` - библиотеки для работы с PostgreSQL
- `curl`, `wget` - инструменты для загрузки файлов

---

### 2.8. Создание системного пользователя

Создайте отдельного пользователя для запуска бота (это повышает безопасность):

```bash
useradd -r -m -s /bin/bash -d /opt/2getpro-v2 2getpro
```

**Что делает команда**: 
- `-r` - создает системного пользователя
- `-m` - создает домашнюю директорию
- `-s /bin/bash` - устанавливает оболочку bash
- `-d /opt/2getpro-v2` - указывает домашнюю директорию
- `2getpro` - имя пользователя

**Ожидаемый результат**: Пользователь создан без вывода сообщений.

---

## 3. Установка бота

### 3.1. Создание директории и клонирование репозитория

Перейдите в директорию установки:

```bash
cd /opt
```

Клонируйте репозиторий бота:

```bash
git clone https://github.com/ваш-username/2GETPRO_v2.git 2getpro-v2
```

❗ **Важно**: Замените `ваш-username` на реальное имя пользователя GitHub или URL вашего репозитория.

**Что делает команда**: Загружает весь код бота с GitHub в директорию `/opt/2getpro-v2`.

**Ожидаемый результат**: Появится прогресс загрузки, затем сообщение о завершении клонирования.

---

### 3.2. Настройка прав доступа

Передайте права на директорию пользователю `2getpro`:

```bash
chown -R 2getpro:2getpro /opt/2getpro-v2
```

**Что делает команда**: Рекурсивно (`-R`) меняет владельца всех файлов и папок на пользователя `2getpro`.

Установите правильные права доступа:

```bash
chmod -R 755 /opt/2getpro-v2
```

**Что делает команда**: Устанавливает права: владелец может читать/писать/выполнять, остальные только читать/выполнять.

---

### 3.3. Создание виртуального окружения Python

Переключитесь на пользователя `2getpro`:

```bash
su - 2getpro
```

**Что делает команда**: Переключает текущего пользователя на `2getpro`.

**Ожидаемый результат**: Приглашение командной строки изменится на `2getpro@server:~$`.

Создайте виртуальное окружение:

```bash
cd /opt/2getpro-v2
python3 -m venv venv
```

**Что делает команда**: Создает изолированное окружение Python в папке `venv`.

**Ожидаемый результат**: Создастся директория `venv` с копией Python.

💡 **Что такое виртуальное окружение?** Это изолированная среда для Python-пакетов, которая не влияет на системные пакеты.

---

### 3.4. Активация виртуального окружения

Активируйте виртуальное окружение:

```bash
source venv/bin/activate
```

**Что делает команда**: Активирует виртуальное окружение.

**Ожидаемый результат**: В начале строки появится `(venv)`, например: `(venv) 2getpro@server:~/2getpro-v2$`.

---

### 3.5. Обновление pip

Обновите менеджер пакетов pip:

```bash
pip install --upgrade pip setuptools wheel
```

**Что делает команда**: Обновляет pip и базовые инструменты до последних версий.

**Ожидаемый результат**: Сообщения об успешном обновлении пакетов.

---

### 3.6. Установка зависимостей

Установите все необходимые Python-пакеты:

```bash
pip install -r requirements.txt
```

**Что делает команда**: Устанавливает все пакеты, перечисленные в файле [`requirements.txt`](requirements.txt:1).

**Ожидаемый результат**: Установка займет 2-5 минут. Вы увидите прогресс установки каждого пакета.

⚠️ **Если возникают ошибки**: 
- Убедитесь, что установлены все системные пакеты из раздела 2.7
- Проверьте подключение к интернету
- Попробуйте установить проблемный пакет отдельно: `pip install имя_пакета`

---

### 3.7. Создание необходимых директорий

Создайте директории для логов и резервных копий:

```bash
mkdir -p logs backups cache
```

**Что делает команда**: Создает три директории (`-p` создает родительские директории при необходимости).

Вернитесь к пользователю root:

```bash
exit
```

**Что делает команда**: Выходит из сессии пользователя `2getpro` обратно к root.

---

## 4. Настройка базы данных

### 4.1. Создание пользователя PostgreSQL

Переключитесь на пользователя postgres:

```bash
su - postgres
```

Создайте пользователя базы данных:

```bash
createuser --interactive --pwprompt 2getpro_user
```

**Что делает команда**: Запускает интерактивный мастер создания пользователя.

Вам будет предложено:
1. **Введите пароль**: Придумайте надежный пароль (например, `SecurePass123!`)
2. **Подтвердите пароль**: Введите пароль еще раз
3. **Суперпользователь?**: Введите `n` (нет)
4. **Создавать базы данных?**: Введите `n` (нет)
5. **Создавать роли?**: Введите `n` (нет)

💡 **Важно**: Запишите пароль - он понадобится при настройке бота!

---

### 4.2. Создание базы данных

Создайте базу данных для бота:

```bash
createdb -O 2getpro_user 2getpro_v2_db
```

**Что делает команда**: 
- Создает базу данных `2getpro_v2_db`
- `-O 2getpro_user` - делает владельцем пользователя `2getpro_user`

**Ожидаемый результат**: База данных создана без вывода сообщений.

---

### 4.3. Проверка подключения к базе данных

Проверьте, что можете подключиться к базе:

```bash
psql -U 2getpro_user -d 2getpro_v2_db -h localhost
```

**Что делает команда**: Подключается к базе данных.

Вам будет предложено ввести пароль. После успешного подключения вы увидите приглашение `2getpro_v2_db=>`.

Выйдите из psql:

```bash
\q
```

Вернитесь к пользователю root:

```bash
exit
```

---

### 4.4. Настройка прав доступа PostgreSQL (опционально)

Если подключение не работает, отредактируйте файл конфигурации:

```bash
nano /etc/postgresql/16/main/pg_hba.conf
```

Найдите строки с `local` и `host` и убедитесь, что они выглядят так:

```
# TYPE  DATABASE        USER            ADDRESS                 METHOD
local   all             all                                     peer
host    all             all             127.0.0.1/32            md5
host    all             all             ::1/128                 md5
```

Сохраните файл (`Ctrl+O`, `Enter`, `Ctrl+X`) и перезапустите PostgreSQL:

```bash
systemctl restart postgresql
```

---

## 5. Конфигурация бота

### 5.1. Создание файла конфигурации

Перейдите в директорию бота:

```bash
cd /opt/2getpro-v2
```

Скопируйте пример конфигурации:

```bash
cp .env.example .env.production
```

**Что делает команда**: Создает файл `.env.production` на основе шаблона [`.env.example`](.env.example:1).

---

### 5.2. Редактирование конфигурации

Откройте файл для редактирования:

```bash
nano .env.production
```

**Что делает команда**: Открывает текстовый редактор nano.

💡 **Как работать в nano**:
- Стрелками перемещайтесь по тексту
- `Ctrl+O` - сохранить
- `Enter` - подтвердить имя файла
- `Ctrl+X` - выйти

---

### 5.3. Заполнение обязательных параметров

Отредактируйте следующие параметры:

#### 🤖 Основные настройки бота

```bash
# Токен вашего бота от @BotFather
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# ID администраторов (через запятую)
ADMIN_IDS=123456789,987654321
```

**Где взять**:
- `BOT_TOKEN`: Создайте бота у [@BotFather](https://t.me/BotFather) командой `/newbot`
- `ADMIN_IDS`: Узнайте свой ID у [@userinfobot](https://t.me/userinfobot)

---

#### 🗄️ Настройки базы данных

```bash
# Параметры подключения к PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=2getpro_v2_db
POSTGRES_USER=2getpro_user
POSTGRES_PASSWORD=ваш_пароль_из_раздела_4.1
```

❗ **Важно**: Замените `ваш_пароль_из_раздела_4.1` на реальный пароль, который вы создали в разделе 4.1.

---

#### 🔴 Настройки Redis

```bash
# Параметры подключения к Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=
REDIS_ENABLED=true
```

💡 **Совет**: Если Redis без пароля, оставьте `REDIS_PASSWORD` пустым.

---

#### 🎛️ Настройки панели управления VPN

```bash
# URL и ключ API вашей панели
PANEL_API_URL=https://your-panel.com/api
PANEL_API_KEY=your_api_key_here

# Лимит трафика для пользователей (в ГБ, 0 = безлимит)
USER_TRAFFIC_LIMIT_GB=0

# Стратегия сброса трафика: NO_RESET, MONTHLY, WEEKLY
USER_TRAFFIC_STRATEGY=NO_RESET
```

**Где взять**:
- `PANEL_API_URL`: URL API вашей панели управления (например, 3x-ui, Marzban)
- `PANEL_API_KEY`: Ключ API из настроек панели

---

#### 💳 Настройки платежных систем

##### YooKassa (основная платежная система)

```bash
# Настройки YooKassa
YOOKASSA_ENABLED=true
YOOKASSA_SHOP_ID=123456
YOOKASSA_SECRET_KEY=live_ваш_секретный_ключ
YOOKASSA_RETURN_URL=https://t.me/ваш_бот

# Автоплатежи (сохранение карт)
YOOKASSA_AUTOPAYMENTS_ENABLED=false
```

**Где взять**:
- Зарегистрируйтесь на [yookassa.ru](https://yookassa.ru)
- `SHOP_ID` и `SECRET_KEY` найдете в личном кабинете

---

##### CryptoPay (криптовалютные платежи)

```bash
# Настройки CryptoPay
CRYPTOPAY_ENABLED=true
CRYPTOPAY_TOKEN=ваш_токен_cryptopay
CRYPTOPAY_NETWORK=mainnet
CRYPTOPAY_ASSET=RUB
```

**Где взять**:
- Создайте приложение в [@CryptoBot](https://t.me/CryptoBot)
- Получите токен в разделе "Crypto Pay API"

---

##### Telegram Stars (встроенные платежи)

```bash
# Telegram Stars (встроенная валюта Telegram)
STARS_ENABLED=true
```

💡 **Совет**: Stars не требуют дополнительной настройки, работают через Telegram.

---

##### FreeKassa (опционально)

```bash
# Настройки FreeKassa
FREEKASSA_ENABLED=false
FREEKASSA_MERCHANT_ID=ваш_merchant_id
FREEKASSA_FIRST_SECRET=секрет_1
FREEKASSA_SECOND_SECRET=секрет_2
FREEKASSA_API_KEY=ваш_api_key
```

---

#### 💰 Цены на подписки

```bash
# Цены в рублях (в копейках: 1 руб = 100 копеек)
RUB_PRICE_1_MONTH=15000    # 150 рублей
RUB_PRICE_3_MONTHS=40000   # 400 рублей
RUB_PRICE_6_MONTHS=75000   # 750 рублей
RUB_PRICE_12_MONTHS=140000 # 1400 рублей

# Цены в Telegram Stars
STARS_PRICE_1_MONTH=150
STARS_PRICE_3_MONTHS=400
STARS_PRICE_6_MONTHS=750
STARS_PRICE_12_MONTHS=1400

# Включение/отключение тарифов
1_MONTH_ENABLED=true
3_MONTHS_ENABLED=true
6_MONTHS_ENABLED=true
12_MONTHS_ENABLED=true
```

💡 **Важно**: Цены в рублях указываются в копейках (умножьте на 100).

---

#### 🎁 Реферальная программа

```bash
# Бонусы для пригласившего (в днях)
REFERRAL_BONUS_DAYS_1_MONTH=3
REFERRAL_BONUS_DAYS_3_MONTHS=7
REFERRAL_BONUS_DAYS_6_MONTHS=15
REFERRAL_BONUS_DAYS_12_MONTHS=30

# Бонусы для приглашенного (в днях)
REFEREE_BONUS_DAYS_1_MONTH=1
REFEREE_BONUS_DAYS_3_MONTHS=3
REFEREE_BONUS_DAYS_6_MONTHS=7
REFEREE_BONUS_DAYS_12_MONTHS=15

# Выдавать бонус только за первую покупку
REFERRAL_ONE_BONUS_PER_REFEREE=true
```

---

#### 🆓 Пробный период

```bash
# Настройки триала
TRIAL_ENABLED=true
TRIAL_DURATION_DAYS=3
TRIAL_TRAFFIC_LIMIT_GB=5.0
```

---

#### 🔔 Уведомления

```bash
# Уведомления о подписках
SUBSCRIPTION_NOTIFICATIONS_ENABLED=true
SUBSCRIPTION_NOTIFY_ON_EXPIRE=true
SUBSCRIPTION_NOTIFY_AFTER_EXPIRE=true
SUBSCRIPTION_NOTIFY_DAYS_BEFORE=3

# Логирование событий в Telegram
LOG_CHAT_ID=
LOG_THREAD_ID=
LOG_NEW_USERS=true
LOG_PAYMENTS=true
LOG_PROMO_ACTIVATIONS=true
LOG_TRIAL_ACTIVATIONS=true
```

💡 **Совет**: Создайте приватный канал для логов и укажите его ID в `LOG_CHAT_ID`.

---

#### 📊 Мониторинг (опционально)

```bash
# Sentry для отслеживания ошибок
SENTRY_ENABLED=false
SENTRY_DSN=
SENTRY_ENVIRONMENT=production

# Prometheus для метрик
PROMETHEUS_ENABLED=true
PROMETHEUS_PORT=9090
```

---

#### 🔒 Безопасность

```bash
# Ограничение частоты запросов
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=30
RATE_LIMIT_WINDOW=60

# Валидация веб-хуков
WEBHOOK_VALIDATION_ENABLED=true
WEBHOOK_SECRET_TOKEN=сгенерируйте_случайную_строку
```

💡 **Совет**: Для генерации секретного токена используйте:
```bash
openssl rand -hex 32
```

---

#### 🌐 Веб-сервер и веб-хуки

```bash
# Настройки веб-сервера
WEB_SERVER_HOST=0.0.0.0
WEB_SERVER_PORT=8080

# Базовый URL для веб-хуков (если используете)
WEBHOOK_BASE_URL=https://ваш-домен.com
```

❗ **Важно**: Если вы планируете использовать веб-хуки, укажите ваш домен с HTTPS.

---

#### 🌍 Локализация

```bash
# Язык по умолчанию
DEFAULT_LANGUAGE=ru
DEFAULT_CURRENCY_SYMBOL=RUB
```

---

#### 📱 Обязательная подписка на канал (опционально)

```bash
# ID канала, на который должны быть подписаны пользователи
REQUIRED_CHANNEL_ID=-1001234567890
REQUIRED_CHANNEL_LINK=https://t.me/ваш_канал
```

**Как получить ID канала**:
1. Добавьте бота [@userinfobot](https://t.me/userinfobot) в канал как администратора
2. Перешлите любое сообщение из канала боту
3. Скопируйте ID канала (начинается с `-100`)

---

### 5.4. Сохранение конфигурации

После заполнения всех параметров:
1. Нажмите `Ctrl+O` для сохранения
2. Нажмите `Enter` для подтверждения
3. Нажмите `Ctrl+X` для выхода

---

### 5.5. Установка прав доступа к конфигурации

Защитите файл конфигурации:

```bash
chown 2getpro:2getpro /opt/2getpro-v2/.env.production
chmod 600 /opt/2getpro-v2/.env.production
```

**Что делает команда**: 
- Устанавливает владельца файла
- Разрешает чтение/запись только владельцу (повышает безопасность)

---

## 6. Настройка systemd сервиса

### 6.1. Создание скриптов запуска

Создайте директорию для скриптов:

```bash
mkdir -p /opt/2getpro-v2/scripts
```

Скопируйте скрипты из репозитория:

```bash
cp /opt/2getpro-v2/infrastructure/systemd/scripts/pre-start.sh /opt/2getpro-v2/scripts/
cp /opt/2getpro-v2/infrastructure/systemd/scripts/graceful-stop.sh /opt/2getpro-v2/scripts/
```

Сделайте скрипты исполняемыми:

```bash
chmod +x /opt/2getpro-v2/scripts/*.sh
```

---

### 6.2. Копирование файла сервиса

Скопируйте файл сервиса в systemd:

```bash
cp /opt/2getpro-v2/infrastructure/systemd/2getpro-v2.service /etc/systemd/system/
```

**Что делает команда**: Копирует конфигурацию сервиса в системную директорию.

---

### 6.3. Редактирование файла сервиса

Откройте файл сервиса для редактирования:

```bash
nano /etc/systemd/system/2getpro-v2.service
```

Убедитесь, что путь к файлу окружения правильный (строка 18):

```ini
EnvironmentFile=/opt/2getpro-v2/.env.production
```

Сохраните и закройте файл (`Ctrl+O`, `Enter`, `Ctrl+X`).

---

### 6.4. Перезагрузка конфигурации systemd

Обновите конфигурацию systemd:

```bash
systemctl daemon-reload
```

**Что делает команда**: Перечитывает все файлы сервисов, включая новый.

**Ожидаемый результат**: Команда выполнится без вывода сообщений.

---

### 6.5. Включение автозапуска

Включите автоматический запуск бота при загрузке системы:

```bash
systemctl enable 2getpro-v2.service
```

**Что делает команда**: Создает символическую ссылку для автозапуска сервиса.

**Ожидаемый результат**: Сообщение `Created symlink...`

---

## 7. Первый запуск

### 7.1. Применение миграций базы данных

Переключитесь на пользователя `2getpro`:

```bash
su - 2getpro
```

Активируйте виртуальное окружение:

```bash
cd /opt/2getpro-v2
source venv/bin/activate
```

Примените миграции базы данных:

```bash
python db/migrator.py
```

**Что делает команда**: Создает все необходимые таблицы в базе данных.

**Ожидаемый результат**: Сообщения о применении миграций:
```
Applying migration: 001_add_balance_system.sql
Migration 001_add_balance_system.sql applied successfully
All migrations applied successfully
```

⚠️ **Если возникла ошибка**: 
- Проверьте правильность параметров подключения к БД в `.env.production`
- Убедитесь, что PostgreSQL запущен: `systemctl status postgresql`
- Проверьте, что база данных создана: `su - postgres -c "psql -l"`

Вернитесь к пользователю root:

```bash
exit
```

---

### 7.2. Запуск бота

Запустите сервис бота:

```bash
systemctl start 2getpro-v2.service
```

**Что делает команда**: Запускает бота как системный сервис.

**Ожидаемый результат**: Команда выполнится без вывода сообщений.

---

### 7.3. Проверка статуса

Проверьте статус сервиса:

```bash
systemctl status 2getpro-v2.service
```

**Ожидаемый результат**: Вы должны увидеть:
- `Active: active (running)` зеленым цветом
- Последние строки логов

💡 **Совет**: Нажмите `q` для выхода из просмотра статуса.

---

### 7.4. Просмотр логов

Посмотрите логи бота в реальном времени:

```bash
journalctl -u 2getpro-v2.service -f
```

**Что делает команда**: 
- `-u 2getpro-v2.service` - показывает логи конкретного сервиса
- `-f` - следит за новыми записями в реальном времени

**Ожидаемый результат**: Вы должны увидеть:
```
Bot started successfully
Connected to database
Redis connection established
Listening for updates...
```

💡 **Совет**: Нажмите `Ctrl+C` для выхода из просмотра логов.

---

### 7.5. Проверка работоспособности

Откройте Telegram и найдите вашего бота. Отправьте команду `/start`.

**Ожидаемый результат**: Бот должен ответить приветственным сообщением.

✅ **Поздравляем! Бот успешно установлен и работает!**

⚠️ **Если бот не отвечает**:
1. Проверьте статус сервиса: `systemctl status 2getpro-v2.service`
2. Посмотрите логи: `journalctl -u 2getpro-v2.service -n 50`
3. Проверьте правильность `BOT_TOKEN` в `.env.production`
4. Убедитесь, что сервер имеет доступ к интернету

---

## 8. Настройка веб-хуков (опционально)

Если вы хотите использовать веб-хуки вместо long polling (рекомендуется для продакшена), выполните следующие шаги.

### 8.1. Установка Nginx

Установите веб-сервер Nginx:

```bash
apt install -y nginx
```

Запустите и включите автозапуск:

```bash
systemctl start nginx
systemctl enable nginx
```

---

### 8.2. Настройка Nginx как reverse proxy

Создайте конфигурацию для бота:

```bash
nano /etc/nginx/sites-available/2getpro-v2
```

Вставьте следующую конфигурацию:

```nginx
server {
    listen 80;
    server_name ваш-домен.com;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

❗ **Важно**: Замените `ваш-домен.com` на ваш реальный домен.

Сохраните файл (`Ctrl+O`, `Enter`, `Ctrl+X`).

Активируйте конфигурацию:

```bash
ln -s /etc/nginx/sites-available/2getpro-v2 /etc/nginx/sites-enabled/
```

Проверьте конфигурацию:

```bash
nginx -t
```

**Ожидаемый результат**: `syntax is ok` и `test is successful`.

Перезапустите Nginx:

```bash
systemctl restart nginx
```

---

### 8.3. Установка SSL сертификата

Установите Certbot для получения бесплатного SSL сертификата:

```bash
apt install -y certbot python3-certbot-nginx
```

Получите сертификат:

```bash
certbot --nginx -d ваш-домен.com
```

**Что делает команда**: Автоматически получает и настраивает SSL сертификат.

Следуйте инструкциям Certbot:
1. Введите email для уведомлений
2. Согласитесь с условиями (введите `Y`)
3. Выберите опцию перенаправления HTTP на HTTPS (рекомендуется `2`)

**Ожидаемый результат**: Сертификат установлен, Nginx перенастроен на HTTPS.

---

### 8.4. Настройка автообновления сертификата

Certbot автоматически настраивает обновление сертификатов. Проверьте:

```bash
systemctl status certbot.timer
```

**Ожидаемый результат**: Таймер активен (`active`).

---

### 8.5. Обновление конфигурации бота

Откройте файл конфигурации:

```bash
nano /opt/2getpro-v2/.env.production
```

Обновите параметр `WEBHOOK_BASE_URL`:

```bash
WEBHOOK_BASE_URL=https://ваш-домен.com
```

Сохраните файл (`Ctrl+O`, `Enter`, `Ctrl+X`).

Перезапустите бота:

```bash
systemctl restart 2getpro-v2.service
```

---

## 9. Мониторинг и обслуживание

### 9.1. Просмотр логов

#### Просмотр последних 100 строк логов:

```bash
journalctl -u 2getpro-v2.service -n 100
```

#### Просмотр логов в реальном времени:

```bash
journalctl -u 2getpro-v2.service -f
```

#### Просмотр логов за сегодня:

```bash
journalctl -u 2getpro-v2.service --since today
```

#### Просмотр логов с определенной даты:

```bash
journalctl -u 2getpro-v2.service --since "2024-01-01"
```

---

### 9.2. Управление сервисом

#### Проверка статуса:

```bash
systemctl status 2getpro-v2.service
```

#### Запуск бота:

```bash
systemctl start 2getpro-v2.service
```

#### Остановка бота:

```bash
systemctl stop 2getpro-v2.service
```

#### Перезапуск бота:

```bash
systemctl restart 2getpro-v2.service
```

#### Перезагрузка конфигурации без остановки:

```bash
systemctl reload 2getpro-v2.service
```

---

### 9.3. Обновление бота

Когда выходит новая версия бота:

1. Остановите бота:
```bash
systemctl stop 2getpro-v2.service
```

2. Переключитесь на пользователя `2getpro`:
```bash
su - 2getpro
```

3. Перейдите в директорию бота:
```bash
cd /opt/2getpro-v2
```

4. Сохраните изменения (если есть):
```bash
git stash
```

5. Загрузите обновления:
```bash
git pull origin main
```

6. Активируйте виртуальное окружение:
```bash
source venv/bin/activate
```

7. Обновите зависимости:
```bash
pip install -r requirements.txt --upgrade
```

8. Примените новые миграции:
```bash
python db/migrator.py
```

9. Вернитесь к root:
```bash
exit
```

10. Запустите бота:
```bash
systemctl start 2getpro-v2.service
```

11. Проверьте статус:
```bash
systemctl status 2getpro-v2.service
```

---

### 9.4. Резервное копирование

#### Создание резервной копии базы данных:

```bash
su - postgres -c "pg_dump 2getpro_v2_db > /tmp/2getpro_backup_$(date +%Y%m%d_%H%M%S).sql"
```

**Что делает команда**: Создает дамп базы данных с текущей датой в имени файла.

#### Восстановление из резервной копии:

```bash
su - postgres -c "psql 2getpro_v2_db < /tmp/2getpro_backup_20240101_120000.sql"
```

💡 **Совет**: Настройте автоматическое резервное копирование через cron:

```bash
crontab -e
```

Добавьте строку для ежедневного бэкапа в 3:00:

```
0 3 * * * su - postgres -c "pg_dump 2getpro_v2_db > /opt/backups/2getpro_$(date +\%Y\%m\%d).sql"
```

---

### 9.5. Мониторинг ресурсов

#### Проверка использования памяти:

```bash
free -h
```

#### Проверка использования диска:

```bash
df -h
```

#### Проверка загрузки процессора:

```bash
top
```

💡 **Совет**: Нажмите `q` для выхода из `top`.

#### Проверка использования ресурсов ботом:

```bash
systemctl status 2getpro-v2.service
```

---

## 10. Решение типичных проблем

### 10.1. Бот не запускается

#### Проблема: Ошибка "Failed to start 2getpro-v2.service"

**Решение**:

1. Проверьте логи:
```bash
journalctl -u 2getpro-v2.service -n 50
```

2. Проверьте синтаксис файла сервиса:
```bash
systemd-analyze verify /etc/systemd/system/2getpro-v2.service
```

3. Проверьте права доступа:
```bash
ls -la /opt/2getpro-v2/.env.production
```

4. Убедитесь, что виртуальное окружение создано:
```bash
ls -la /opt/2getpro-v2/venv
```

---

### 10.2. Проблемы с подключением к базе данных

#### Проблема: "could not connect to server"

**Решение**:

1. Проверьте, что PostgreSQL запущен:
```bash
systemctl status postgresql
```

2. Проверьте параметры подключения в `.env.production`:
```bash
grep POSTGRES /opt/2getpro-v2/.env.production
```

3. Попробуйте подключиться вручную:
```bash
psql -U 2getpro_user -d 2getpro_v2_db -h localhost
```

4. Проверьте логи PostgreSQL:
```bash
tail -f /var/log/postgresql/postgresql-16-main.log
```

---

### 10.3. Проблемы с Redis

#### Проблема: "Error connecting to Redis"

**Решение**:

1. Проверьте, что Redis запущен:
```bash
systemctl status redis-server
```

2. Проверьте подключение:
```bash
redis-cli ping
```

3. Если Redis требует пароль:
```bash
redis-cli -a ваш_пароль ping
```

4. Перезапустите Redis:
```bash
systemctl restart redis-server
```

---

### 10.4. Проблемы с правами доступа

#### Проблема: "Permission denied"

**Решение**:

1. Проверьте владельца файлов:
```bash
ls -la /opt/2getpro-v2/
```

2. Исправьте права доступа:
```bash
chown -R 2getpro:2getpro /opt/2getpro-v2
chmod -R 755 /opt/2getpro-v2
chmod 600 /opt/2getpro-v2/.env.production
```

3. Проверьте права на директорию логов:
```bash
ls -la /var/log/2getpro
chown -R 2getpro:2getpro /var/log/2getpro
```

---

### 10.5. Проблемы с зависимостями Python

#### Проблема: "ModuleNotFoundError: No module named 'xxx'"

**Решение**:

1. Активируйте виртуальное окружение:
```bash
su - 2getpro
cd /opt/2getpro-v2
source venv/bin/activate
```

2. Переустановите зависимости:
```bash
pip install -r requirements.txt --force-reinstall
```

3. Если проблема с конкретным пакетом:
```bash
pip install имя_пакета --upgrade
```

---

### 10.6. Бот не отвечает на команды

#### Проблема: Бот онлайн, но не отвечает

**Решение**:

1. Проверьте логи на ошибки:
```bash
journalctl -u 2getpro-v2.service -n 100
```

2. Проверьте правильность токена:
```bash
grep BOT_TOKEN /opt/2getpro-v2/.env.production
```

3. Убедитесь, что ваш ID в списке администраторов:
```bash
grep ADMIN_IDS /opt/2getpro-v2/.env.production
```

4. Перезапустите бота:
```bash
systemctl restart 2getpro-v2.service
```

---

### 10.7. Проблемы с веб-хуками

#### Проблема: Веб-хуки не работают

**Решение**:

1. Проверьте, что Nginx запущен:
```bash
systemctl status nginx
```

2. Проверьте конфигурацию Nginx:
```bash
nginx -t
```

3. Проверьте SSL сертификат:
```bash
certbot certificates
```

4. Проверьте логи Nginx:
```bash
tail -f /var/log/nginx/error.log
```

5. Убедитесь, что порт 8080 открыт:
```bash
netstat -tulpn | grep 8080
```

---

### 10.8. Высокое использование памяти

#### Проблема: Бот использует слишком много памяти

**Решение**:

1. Проверьте использование памяти:
```bash
systemctl status 2getpro-v2.service
```

2. Настройте лимит памяти в файле сервиса:
```bash
nano /etc/systemd/system/2getpro-v2.service
```

Измените строку:
```ini
MemoryMax=2G
```

3. Перезагрузите конфигурацию:
```bash
systemctl daemon-reload
systemctl restart 2getpro-v2.service
```

---

### 10.9. Проблемы с миграциями

#### Проблема: Ошибка при применении миграций

**Решение**:

1. Проверьте текущую версию схемы:
```bash
su - postgres -c "psql 2getpro_v2_db -c 'SELECT * FROM alembic_version;'"
```

2. Откатите последнюю миграцию:
```bash
su - 2getpro
cd /opt/2getpro-v2
source venv/bin/activate
python -m alembic downgrade -1
```

3. Примените миграции заново:
```bash
python -m alembic upgrade head
```

---

### 10.10. Бот падает после запуска

#### Проблема: Сервис запускается, но сразу падает

**Решение**:

1. Проверьте полные логи:
```bash
journalctl -u 2getpro-v2.service -n 200 --no-pager
```

2. Запустите бота вручную для отладки:
```bash
su - 2getpro
cd /opt/2getpro-v2
source venv/bin/activate
python main.py
```

3. Проверьте все обязательные переменные окружения:
```bash
grep -E "BOT_TOKEN|POSTGRES|REDIS|PANEL_API" /opt/2getpro-v2/.env.production
```

---

## 📞 Получение помощи

Если вы столкнулись с проблемой, которая не описана в этом руководстве:

1. **Проверьте логи**: `journalctl -u 2getpro-v2.service -n 100`
2. **Проверьте документацию**: Посмотрите другие файлы в директории `docs/`
3. **Создайте issue**: Опишите проблему на GitHub с приложением логов
4. **Обратитесь в поддержку**: Свяжитесь с разработчиками

---

## ✅ Чек-лист успешной установки

Убедитесь, что выполнены все пункты:

- [ ] Ubuntu 24.04.03 LTS установлена и обновлена
- [ ] Python 3.11+ установлен
- [ ] PostgreSQL установлен и запущен
- [ ] Redis установлен и запущен
- [ ] База данных создана и настроена
- [ ] Код бота клонирован в `/opt/2getpro-v2`
- [ ] Виртуальное окружение создано
- [ ] Все зависимости установлены
- [ ] Файл `.env.production` настроен
- [ ] Миграции базы данных применены
- [ ] Systemd сервис настроен и запущен
- [ ] Бот отвечает на команду `/start`
- [ ] Логи не содержат критических ошибок

---

## 🎉 Поздравляем!

Вы успешно установили Telegram-бота 2GETPRO v2 на ваш сервер!

Теперь вы можете:
- Принимать платежи от пользователей
- Управлять подписками
- Интегрироваться с панелью управления VPN
- Использовать реферальную программу
- Предоставлять пробные периоды

**Следующие шаги**:
1. Протестируйте все функции бота
2. Настройте мониторинг (Prometheus, Grafana)
3. Настройте резервное копирование
4. Изучите документацию по администрированию

---

## 📚 Дополнительные ресурсы

- [Документация по API](../api/README.md)
- [Руководство по Docker](docker.md)
- [Руководство по Kubernetes](kubernetes.md)
- [Руководство по продакшену](production.md)
- [Решение проблем](../operations/troubleshooting.md)

---

**Версия документа**: 1.0  
**Дата обновления**: 2024-11-27  
**Автор**: 2GETPRO Team